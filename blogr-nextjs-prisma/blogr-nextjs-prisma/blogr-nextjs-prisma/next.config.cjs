/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Ajuste si tu utilises des images distantes :
  images: { remotePatterns: [] },
};
module.exports = nextConfig;
